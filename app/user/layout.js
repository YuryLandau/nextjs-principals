

export const metadata = {
    title: 'User'
}

const UserLayout = ({ children }) => {
    return (
        <div>
            <p>
                UserLayout
            </p>
            {children}
        </div>
    )
}

export default UserLayout